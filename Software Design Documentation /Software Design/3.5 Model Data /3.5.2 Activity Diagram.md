### 3.5.2 Activity Diagram

*Activity Diagram* ini memodelkan aliran kerja dinamis (*dynamic workflow*) dan logika operasional dari sistem **Sapopoe**. Untuk memberikan representasi teknis yang akurat, diagram ini mengimplementasikan arsitektur *Swimlane* (kolom partisi) yang memisahkan batas tanggung jawab antara aktivitas di sisi klien (**Pengguna/Frontend**) dan pemrosesan logika di sisi server (**Sistem/Backend Laravel**).

#### A. Skenario 1: Registrasi, Verifikasi OTP, dan Inisialisasi Dompet
Skenario ini memetakan alur keamanan awal. Sistem mewajibkan verifikasi identitas (OTP via Gmail) dan memastikan tidak ada pengguna yang dapat mengakses *Dashboard* sebelum mereka mendaftarkan dompet keuangan pertama mereka.

```mermaid
flowchart TD
    %% Styling Bentuk Node standar UML
    classDef startNode fill:#000,stroke:#000,stroke-width:2px,color:#fff;
    classDef endNode fill:#fff,stroke:#000,stroke-width:4px;
    classDef activity fill:#ffffff,stroke:#333,stroke-width:1.5px;
    
    %% SWIMLANE: PENGGUNA (FRONTEND)
    subgraph Pengguna [Pengguna Aplikasi / Frontend]
        direction TB
        Start1(( )):::startNode
        Reg(Melakukan Registrasi Akun):::activity
        InpOTP(Memasukkan Kode OTP):::activity
        InpDompet(Menginput Saldo Awal Dompet):::activity
        Dash(Mengakses Halaman Dashboard):::activity
        End1((( ))):::endNode
    end
    
    %% SWIMLANE: SISTEM (BACKEND LARAVEL)
    subgraph Sistem [Sistem / Backend Laravel]
        direction TB
        Kirim(Kirim Kode OTP ke Email):::activity
        Verif(Memproses Verifikasi OTP):::activity
        CekOTP{Kode OTP Valid?}
        CekDompet{Sudah Punya Dompet?}
        SimpanDompet(Menyimpan Data Dompet):::activity
    end
    
    %% ALUR RELASI (WORKFLOW)
    Start1 --> Reg
    Reg --> Kirim
    Kirim --> InpOTP
    InpOTP --> Verif
    Verif --> CekOTP
    
    CekOTP -- Tidak --> InpOTP
    CekOTP -- Ya --> CekDompet
    
    CekDompet -- Belum --> InpDompet
    InpDompet --> SimpanDompet
    SimpanDompet --> Dash
    
    CekDompet -- Sudah --> Dash
    Dash --> End1

    #### B. Skenario 2: Pencatatan Pengeluaran & Pengecekan Budget (Strict Balance)
    flowchart TD
    %% Styling Bentuk Node standar UML
    classDef startNode fill:#000,stroke:#000,stroke-width:2px,color:#fff;
    classDef endNode fill:#fff,stroke:#000,stroke-width:4px;
    classDef activity fill:#ffffff,stroke:#333,stroke-width:1.5px;

    %% SWIMLANE: PENGGUNA (FRONTEND)
    subgraph Pengguna [Pengguna Aplikasi / Frontend]
        direction TB
        Start2(( )):::startNode
        MenuExp(Memilih Form Pengeluaran):::activity
        InputExp(Menginput Nominal & Kategori):::activity
        NotifErr(Menerima Error: Saldo Kurang):::activity
        NotifWarn(Menerima Notifikasi Overbudget):::activity
        ViewDash(Melihat Pembaruan Grafik):::activity
        End2((( ))):::endNode
    end

    %% SWIMLANE: SISTEM (BACKEND LARAVEL)
    subgraph Sistem [Sistem / Backend Laravel]
        direction TB
        CekSaldo{Saldo >= Nominal?}
        Tolak(Batalkan & Kembalikan Error):::activity
        Potong(Potong Saldo Dompet):::activity
        SimpanExp(Simpan Data Transaksi):::activity
        CekBudget{Melebihi Limit Budget?}
        KirimWarn(Kirim Peringatan Merah):::activity
        UpdateGrafik(Re-kalkulasi Data Grafik):::activity
    end

    %% ALUR RELASI (WORKFLOW)
    Start2 --> MenuExp
    MenuExp --> InputExp
    InputExp --> CekSaldo
    
    %% Cabang 1: Saldo Ditolak
    CekSaldo -- Tidak --> Tolak
    Tolak --> NotifErr
    NotifErr --> End2
    
    %% Cabang 2: Saldo Diterima
    CekSaldo -- Ya --> Potong
    Potong --> SimpanExp
    SimpanExp --> CekBudget
    
    %% Evaluasi Budget
    CekBudget -- Ya --> KirimWarn
    KirimWarn --> NotifWarn
    NotifWarn --> UpdateGrafik
    
    CekBudget -- Tidak --> UpdateGrafik
    
    UpdateGrafik --> ViewDash
    ViewDash --> End2

    C. Skenario 3: Manajemen Profil & Pemrosesan Gambar (WebP)
    flowchart TD
    %% Styling Bentuk Node standar UML
    classDef startNode fill:#000,stroke:#000,stroke-width:2px,color:#fff;
    classDef endNode fill:#fff,stroke:#000,stroke-width:4px;
    classDef activity fill:#ffffff,stroke:#333,stroke-width:1.5px;

    %% SWIMLANE: PENGGUNA (FRONTEND)
    subgraph Pengguna [Pengguna Aplikasi / Frontend]
        direction TB
        Start3(( )):::startNode
        MenuProf(Mengakses Pengaturan Profil):::activity
        UpFoto(Mengunggah File Foto Asli):::activity
        ViewFoto(Melihat Avatar Baru):::activity
        End3((( ))):::endNode
    end

    %% SWIMLANE: SISTEM (BACKEND LARAVEL)
    subgraph Sistem [Sistem / Backend Laravel]
        direction TB
        TerimaFoto(Menerima Raw File):::activity
        Crop(Otomatis Crop & Konversi WebP):::activity
        SimpanDB(Simpan ke Storage Lokal & DB):::activity
    end

    %% ALUR RELASI (WORKFLOW)
    Start3 --> MenuProf
    MenuProf --> UpFoto
    UpFoto --> TerimaFoto
    TerimaFoto --> Crop
    Crop --> SimpanDB
    SimpanDB --> ViewFoto
    ViewFoto --> End3
